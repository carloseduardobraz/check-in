{
  "name": "nome-do-seu-projeto",
  "version": "1.0.0",
  "description": "Sistema de confirmações com persistência no PostgreSQL",
  "main": "server.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.0.0",
    "pg": "^8.11.0"
  }
}